{
  "name": "pocketshell-web",
  "version": "0.1.0",
  "description": "Web port of PocketShell — Google login, synced hosts, browser terminal over a Lambda SSH bridge.",
  "license": "MIT",
  "author": "Alexey Grigorev <alexey.s.grigoriev@gmail.com>",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/build-blog.mjs --list && vue-tsc -p tsconfig.json --noEmit && vite build && node scripts/build-blog.mjs",
    "preview": "vite preview",
    "sync:github-history": "python3 scripts/sync-gh-history.py",
    "typecheck": "vue-tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@xterm/addon-fit": "0.11.0",
    "@xterm/xterm": "6.0.0",
    "pinia": "2.3.0",
    "vue": "3.5.13",
    "vue-router": "4.5.0"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "@vitejs/plugin-vue": "5.2.1",
    "marked": "18.0.12",
    "typescript": "5.7.2",
    "vite": "6.0.7",
    "vitest": "^2.1.9",
    "vue-tsc": "2.2.0"
  }
}
